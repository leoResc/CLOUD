Given /^Admin clicks on the button 'New Event' on the admin\-site$/ do
  pending # express the regexp above with the code you wish you had
end

When /^admin types in a valid event name$/ do
  pending # express the regexp above with the code you wish you had
end

When /^admin types in a valid event password$/ do
  pending # express the regexp above with the code you wish you had
end

When /^admin sets a valid beginning date$/ do
  pending # express the regexp above with the code you wish you had
end

When /^admin sets a valid ending date$/ do
  pending # express the regexp above with the code you wish you had
end

When /^admin chooses valid number of playlists$/ do
  pending # express the regexp above with the code you wish you had
end

When /^admin hits the submit button 'ON'$/ do
  pending # express the regexp above with the code you wish you had
end

Then /^A new event gets created$/ do
  pending # express the regexp above with the code you wish you had
end

Then /^All the data is stored in our database$/ do
  pending # express the regexp above with the code you wish you had
end

When /^admin types in an invalid event name$/ do
  pending # express the regexp above with the code you wish you had
end

Then /^An error message gets displayed to the admin that the event name already exists or empty field$/ do
  pending # express the regexp above with the code you wish you had
end

Then /^Nothing is stored in our database And No event gets created$/ do
  pending # express the regexp above with the code you wish you had
end

When /^admin sets an invalid beginning date$/ do
  pending # express the regexp above with the code you wish you had
end

Then /^An error message gets displayed to the admin that the beginning date is incorrect$/ do
  pending # express the regexp above with the code you wish you had
end

When /^admin sets an invalid ending date$/ do
  pending # express the regexp above with the code you wish you had
end

Then /^An error message gets displayed to the admin that the ending date is incorrect$/ do
  pending # express the regexp above with the code you wish you had
end

When /^admin chooses invalid number of playlists$/ do
  pending # express the regexp above with the code you wish you had
end

Then /^An error message gets displayed to the admin that he has to choose at least one playlist$/ do
  pending # express the regexp above with the code you wish you had
end

Given /^Casual user clicks on the button 'login' or hits 'ENTER' on the login\-site$/ do
  pending # express the regexp above with the code you wish you had
end

When /^Casual user types in a valid nickname$/ do
  pending # express the regexp above with the code you wish you had
end

When /^The user types in a valid \(event\) password$/ do
  pending # express the regexp above with the code you wish you had
end

When /^The user accepts our terms of use$/ do
  pending # express the regexp above with the code you wish you had
end

When /^The user hits the submit button$/ do
  pending # express the regexp above with the code you wish you had
end

Then /^A new \(valid for the current event only\) account gets created$/ do
  pending # express the regexp above with the code you wish you had
end

When /^The user types in an invalid \(event\) password$/ do
  pending # express the regexp above with the code you wish you had
end

Then /^An error message gets displayed to the user that the nickname or password is invalid$/ do
  pending # express the regexp above with the code you wish you had
end

Then /^Nothing is stored in our database$/ do
  pending # express the regexp above with the code you wish you had
end

Then /^No account gets created$/ do
  pending # express the regexp above with the code you wish you had
end

When /^Casual user types in an invalid nickname$/ do
  pending # express the regexp above with the code you wish you had
end

Then /^An error message gets displayed to the user that the nickname already exists$/ do
  pending # express the regexp above with the code you wish you had
end

When /^The user doesn't accept our terms of use$/ do
  pending # express the regexp above with the code you wish you had
end

Then /^An error message gets displayed to the user that he has to accept terms of use$/ do
  pending # express the regexp above with the code you wish you had
end

Given /^Casual user clicks on the button 'sign in'$/ do
  pending # express the regexp above with the code you wish you had
end

When /^The admin types in an the nickname 'admin'$/ do
  pending # express the regexp above with the code you wish you had
end

When /^The admin types in the password 'cloud'$/ do
  pending # express the regexp above with the code you wish you had
end

When /^The admin hits again the 'sign in' button$/ do
  pending # express the regexp above with the code you wish you had
end

Then /^The admin gets forwarded to the index$/ do
  pending # express the regexp above with the code you wish you had
end
